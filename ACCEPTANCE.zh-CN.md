# 声迹验收记录

## 1.3.6（18）实时字幕完成段落停留

- Apple Speech 交付最终段落后，将该段作为滑动上下文保留约 2 秒。
- 逗号、句号或 `isFinal` 边界不会让悬浮窗立即清空；保留期间后续模型结果会立即拼接并继续滑动，不冻结显示。
- 停留结束后直接跟进最新连续输出，不重新启用短句截取；滑动窗口仍限制为约 15 Hz。

## 1.3.5（17）连续滑动实时字幕与公开仓库

- 取消实时字幕短句尾部截取和 750 ms 人工分段；Apple Speech 的正常增量结果持续更新，即使没有逗号或句号也不会强制切句。
- 悬浮窗按从左到右阅读顺序追加内容，窗口跟随文本尾部向前滑动。
- 显示刷新限制为约 15 Hz，按积压字符数每次推进 1–6 个字符；不使用持续 60 fps 跑马灯动画，降低 60 Hz 屏幕拖影。
- 系统“减少动态效果”开启时直接显示当前完整结果，不执行滑动。
- 设置中的 GitHub 页面指向 `https://github.com/maddylaneeee/ShengJi`。
- README、许可证、第三方声明和 Git 忽略规则已按公开发布仓库重写。

## 1.3.4（16）短句实时字幕、稿件编辑工具与字幕导入（历史版本）

- 实时字幕不再把持续识别结果堆成三四行：优先使用 Apple `isFinal` 语音段落边界，并结合约 750 ms 更新停顿、标点、连接词及可读宽度显示当前短句；没有逗号或句号时仍会限制为单行可读尾句。
- 暂停和完成页改用原生 macOS 文本编辑视图，保留选择、键盘编辑与撤销；新增循环查找、替换所选、删除所选，以及删除当前节点之前/之后、节点 A 与 B 之间的全部文字。
- 首页将“导入稿件”置于“转录后翻译”左侧并各占半宽；支持 SRT、WebVTT、TXT、Markdown 与声迹 JSON。
- 导入后可直接进入完成态编辑，也可保留现有文本和时间轴，进入麦克风准备页，等待手动点击“开始转录”后继续追加。
- 自动化覆盖无标点实时字幕缩短、SRT/VTT 解析、时间戳保留、查找替换与节点范围删除。

## 1.3.3（15）流式逐字动画、麦克风准确性与 Apple 实时字幕

- Apple 与 Whisper 的文件和麦克风增量结果使用自适应逐字动画；速度按最近输出间隔和当前待显示字符数调整，结束时立即补齐，不延迟保存和导出。
- Whisper 麦克风不再每累计约 4 秒机械截断；至少 3 秒后等待约 550 ms 自然停顿，连续讲话最长 12 秒回退，并保留跨块上下文。
- 麦克风专用过滤器删除“请不吝点赞、订阅、转发、打赏支持明镜与点点栏目”及繁体/标点变体，过滤孤立的“谢谢大家”“Thank you”等结束语，并抑制相邻块的完全重复。
- 文件转写仍使用音频能量感知的末尾过滤，真实说出的结束语不会因麦克风规则而被误删。
- Apple 实时字幕移除第三方模型和模型名称 UI，固定使用 Apple SpeechAnalyzer；输入缓冲增加短时余量，停止时等待分析器和最终结果自然完成，避免丢失尾句。
- 自动化覆盖逐字非瞬时显示、速度边界、自然停顿分块、推广套话跨片段过滤和连续重复过滤。

## 1.3.2（14）Whisper 增量输出、阶段进度与末尾静音幻觉过滤

- 已在 1.3.1 实际 GUI 中复现：38 秒文件转写期间进度停留在约 12%，直到整段 `whisper_full` 完成后才一次性显示文字；模型并未卡死，但 UI 无法说明当前工作阶段。
- 接入 whisper.cpp `new_segment_callback`：较长文件每完成一批内部窗口片段即可立即显示，最终结果仍由完整置信度与幻觉过滤结果替换。
- 文件状态分为“加载识别模型”“准备/转换音频”“VAD 与 Whisper 推理”“过滤幻觉并整理文字”；无可靠百分比时显示不确定进度，而不是停在固定数值。
- “谢谢大家”“感谢观看”等仅在最后短片段且对应原音频区间无语音能量时删除；真实说出的结束语保留。
- 主界面按钮、菜单、选择器统一使用轻量强调色悬停边框；两个入口卡片不再整块变成高饱和蓝色。
- 105.5 秒实际 GUI 长文件回归中，进度 36% 时已显示 136 字、72% 时显示 268 字，完成后为 366 字，证明不再等待整段推理结束才出文字。
- 尾部 25 秒静音素材完成后未出现“谢谢大家”；另一条真实说出“今天的会议到这里结束。谢谢大家。”的素材仍完整保留结束语。
- 保留测试素材目录：`/Users/mattlixinchen/Desktop/Files/声迹测试素材`。

## 1.3.1（13）Whisper VAD、整段输入与幻觉过滤

- Whisper 文件转录不再使用应用层固定、无重叠的 28 秒切片；16 kHz 单声道 PCM 整段交给 whisper.cpp，由其内部推进约 30 秒窗口。
- 随应用打包官方 `ggml-silero-v6.2.0.bin`；12 秒以上文件自适应启用 VAD，参数为 250 ms 最短语音、500 ms 结束静音、28 秒最长语音、250 ms 语音填充与 250 ms 重叠。
- 音频转换使用最高质量采样率转换，并清理非有限样本、直流偏移和削波；不会自动放大背景噪声。
- 准确模式恢复 0.2 温度步进回退，并联合无语音概率、文本 token 平均概率、机械重复和连续重复片段过滤幻觉。
- 合成“5 秒静音 + 英文语音 + 尾部静音”实际转录通过：VAD 只保留 4.77–12.63 秒语音，音频量减少 60.7%，输出两段正确文本且时间戳映射回原音频。

## 1.3.0（12）模型、结果页与设置优化

- 全新安装默认使用 Apple Speech Framework；第三方模型只在用户主动选择后启用，并在下次启动恢复用户最后一次选择。
- 首页与转录准备页统一为“默认 / 第三方模型”两级选择，第三方模型名称使用独立自适应控件，避免长名称与图标重叠。
- “完成后自动翻译”生成结果后自动切换到译文；手动重新翻译完成后也采用相同行为。
- 文件和麦克风转录完成页新增“重新开始”，可选择保存当前原文/译文或不保存，并返回等待手动开始的新转录页面。
- 导出面板新增“复制到剪贴板”。
- 设置“通用”移除旧“转录”条目，新增登录自启动、Finder 定位与卸载；保留隐私说明。
- 相关文档改为 lixinchen.ca 站内页面并由默认浏览器打开；新增 GitHub 页面入口。
- 移除 UI 中的计算后端选项：Apple 由系统管理；SenseVoice/Parakeet 自动尝试 Core ML 后回退 CPU；Whisper 自动尝试 Metal 后回退 CPU。

## 1.2.5（11）优化验收范围

目标：macOS 15.5+、Apple Silicon arm64；Apple SpeechAnalyzer 功能在 macOS 26+ 可用。

- 翻译按稳定片段 ID、时间轴和换行对应；空结果、数量不匹配与重试失败使用明确回退标记。
- 活动转录使用有界 `LazyVStack`；默认自动跟随，用户上滚后暂停，并提供“回到最新内容”。
- 完整片段、翻译和事件写入会话 JSONL journal；恢复快照 v2 继续兼容 v1。
- Whisper 直接调用 whisper.cpp v1.9.1 API；Metal 失败回退 CPU，不把普通 GGML Metal 声称为 ANE。
- SenseVoice/Parakeet 自动模式维持 CPU；显式 Core ML 模式标记为系统选择并在失败时回退 CPU。
- NLLB 保持独立 CTranslate2 CPU/int8 运行时。
- macOS 26 Apple Speech 类型通过可用性边界隔离；Deployment Target、生成器、Info.plist、测试和 Whisper 静态库统一为 15.5。
- 发布前要求 Debug、Release、Analyze、单元测试、严格嵌套签名、临时目录解压验签、CLI/helper 启动、真实音频和视频转写通过。

## 1.2.4（10）历史验收记录

日期：2026-07-06
目标：macOS 26+、Apple Silicon arm64、本机运行

## 1.2.4（10）长视频完整性、编辑器与响应性验收

- 用户现场恢复快照复核：`突围第24集.mp4` 总时长 44:14、输入进度 100%，旧版却只有 75 个最终片段，最后时间戳停在 15:17，确认不是导出器丢字幕，而是 Apple Speech 结果收集被固定 450 ms 延迟后取消。
- 完成流程改为在 `SpeechAnalyzer.finalizeAndFinishThroughEndOfInput()` 后等待 `SpeechTranscriber.results` 自然结束，不再用定时器截断仍在排队的最终结果。
- 180 秒视频回归：10 个最终片段，最后时间戳 179.999 秒；GUI 恢复快照同样为 10 行、10 个片段并覆盖到 179.999 秒。
- 30 分钟视频压力测试：GUI 在识别中可立即开关检查器；完成后共 94 行、94 个最终片段，最后时间戳 1799.592 秒，覆盖整个 1799.593 秒音轨。
- Apple 最终识别片段现在逐行同步到编辑器；正文使用 19 pt 字号、8 pt 行距和最大 900 pt 阅读宽度，长转录不再挤成连续文字墙。
- 临时结果最多每 120 ms 刷新一次；文件读取进度最多每 100 ms 且至少推进 0.25% 才刷新，避免长文件数万次缓冲区更新淹没主线程。
- 恢复快照写入改为代次校验并通过 actor 串行保存，避免旧的延迟写入覆盖较新的进度。
- Debug、Release 与 Xcode Analyze 均通过；Apple Speech 独立烟雾测试及 Release GUI 长视频实测通过。

## 1.2.3（9）NLLB 模型下载与清理验收

- 转录前与转录结果页选择 NLLB、且本机未安装模型时，现会显示“下载模型”按钮、约 630.5 MB 的大小提示，并保持“生成译文”禁用直到模型就绪。
- 下载过程显示总体进度和“取消”按钮；GUI 实测下载从 0% 正常推进至 13%，取消后恢复为未安装状态且不会遗留未完成的大文件。
- 模型下载锁定到 `osa911/nllb-200-distilled-600M-ct2-int8` 的固定提交，四个必需文件均校验预期字节数和 SHA-256，避免上游文件变更造成静默损坏。
- 支持新仓库的 `sentencepiece.bpe.model` / `shared_vocabulary.json`，同时兼容老版本的 `flores200_sacrebleu_tokenizer_spm.model` / `shared_vocabulary.txt`。
- 找到老版本遗留的 Distilled 600M INT8 模型后已迁移到新统一目录；新版 GUI 正确显示“NLLB 本机模型翻译”，无需重复下载。
- 通过新版 GUI 对恢复快照中的 390 字符英文转录执行 NLLB → 简体中文，译文成功生成、可切换显示并加入恢复快照。
- 删除旧版本不再使用的 NLLB 1.2B INT8 模型，释放约 1.3 GB；删除桌面安装包、工作区 `dist` 与 `Vendor` 中三个空的 `LocalScribeNLLB 2` 遗留目录。
- 清理后桌面现有 1.2.2（8）普通深度签名校验仍通过；Whisper、SenseVoice 与 Parakeet 模型目录未改动。
- Debug、Release 与最终 Xcode Analyze 均通过；唯一警告为工程未使用 AppIntents 时 Xcode 跳过元数据提取。
- 1.2.3（9）已发布到正式更新地址；线上 manifest 版本、ZIP 大小和 SHA-256 与本地一致，线上 ZIP 与本地产物逐字节一致，解压后严格签名校验通过。
- 按用户要求，本轮仅发布更新，未替用户安装；桌面 `/Users/mattlixinchen/Desktop/Apps/声迹.app` 保持 1.2.2（8），可在“设置 → 更新”手动升级。

## 1.2.2（8）功能与回归验收

- 实时字幕翻译入口已暂时关闭；翻译控制、调用与状态处理源码仍保留，并由单一功能开关隔离，便于后续恢复。
- 麦克风和文件转录完成后的结果页支持 Apple Translation 与 NLLB 两种翻译服务；Apple 为默认选项。
- NLLB 本机运行时已随应用打包，运行时健康检查通过；未安装 NLLB 模型时会明确显示“模型未安装”并禁用生成译文，不会误报成功。
- Apple Translation 英译中 CLI 回归通过，输出“你好，世界，这是一个翻译测试。”。
- 主页面顶部的全局语言选项已删除；麦克风和文件识别语言保留在各自二级页面，实时字幕卡片新增独立“字幕语言”下拉菜单。
- 实时字幕选择 Whisper 后，模型菜单被拆分到独立自适应行；GUI 实测未再出现控件或文字重叠。
- 实时字幕页不再显示翻译、目标语言和原文显示开关，并明确提示实时字幕翻译已暂时关闭。
- GUI 回归通过：结果页 Apple 默认、NLLB 可选；切换到未安装模型的 NLLB 后旧 Apple 译文会清除，切回 Apple 后可重新翻译。
- 工程重新生成、Debug 构建、Release 构建和 Xcode Analyze 均通过。
- Release ZIP 解压后版本为 1.2.2（8），`codesign --verify --deep --strict` 严格签名校验通过。
- 按用户反馈，本轮未重复执行 Whisper 实际转写质量测试；已有 1.2.1 的 Whisper 文件与 Mac 系统声音验收结果保留在下文。
- 更新服务发布完成：MLCCS 上原 1.2.1（7）清单与 ZIP 已备份，线上 `update.json` 和 ZIP 已更新为 1.2.2（8）。
- 线上 ZIP 重新下载后为 20,802,304 字节、SHA-256 为 `b4b4b66a136851ff3f49e80ad599d7a1ab7531966e95b0c7c947f0fa4928002a`，并与本地发布包逐字节一致。
- 桌面现有 1.2.1（7）已通过“检查更新 → 下载更新 → 安装并重新打开”升级到 1.2.2（8）；未使用备用覆盖方案。
- 更新后的桌面版已自动重新打开，首页新版布局显示正常；再次检查更新显示“当前已是最新版本”。
- 桌面安装版为 arm64；其文件内容与线上 ZIP 解压应用经 `rsync -rlcn` 校验无差异，普通深度签名校验通过，线上 ZIP 在非 FileProvider 路径下的 `codesign --verify --deep --strict` 严格签名校验通过。桌面/iCloud FileProvider 会在清理后数秒内为 `.app` 与 `.framework` 重新附加仅含 bundle 标志（`0x2000`）的 FinderInfo，因此安装位置的严格校验不能长期保持通过；该同步元数据不改变包内文件或代码签名。
- 桌面安装版 Apple Translation CLI 英译中通过，输出“你好，世界，这是一个已安装的应用程序翻译测试。”。
- 桌面安装版内置 NLLB/CTranslate2 运行时健康检查通过，运行时版本为 4.6.2；由于本机未安装 NLLB 模型，本轮未执行 NLLB 实际译文质量测试。

## 1.2.1（7）热修复验收

- 针对用户反馈“Apple 转写效果一般，暂不需要测试 Whisper；翻译功能实测好像不可用”，本轮只补验翻译链路与结果页交互，未重新进行 Whisper 转写验收。
- 通过最小 Swift 程序确认 Apple `Translation` Framework 的本机 `TranslationSession(installedSource:target:)` 可用，英译中输出正常。
- 桌面版 CLI 翻译入口通过：`/Users/mattlixinchen/Desktop/Apps/声迹.app/Contents/MacOS/LocalScribe --cli translate ... --source en_US --target zh-Hans` 输出中文译文。
- 修复结果页目标语言切换后的旧译文残留：当已生成译文后切换目标语言，会自动清除旧状态并重新调用 Apple Translation。
- 修复“目标语言与原文相同”时看起来像翻译失效的问题：现在会显示“目标语言与原文相同”，禁用“生成译文”，并清空旧译文。
- Debug 版 GUI 验证通过：日文译文切换到简体中文后自动重译；切换到 English（与原文相同）后提示并禁用翻译。
- Release 安装版 GUI 验证通过：`/Users/mattlixinchen/Desktop/Apps/声迹.app` 在结果预览页从 English 切换到简体中文后自动本地翻译，译文页显示“有些人说，幸福的秘诀是没有期望。识别完成后，成绩单仍可编辑。”
- 桌面版更新流程通过：旧版 1.2（6）在“设置 → 更新”检测到 1.2.1（7），完成下载、安装并自动重新打开；更新后再次检查显示“当前已是最新版本”。
- 桌面最终安装版本已复核为 `/Users/mattlixinchen/Desktop/Apps/声迹.app` 1.2.1（7）。
- 桌面最终安装版已清理 FinderInfo 扩展属性，`codesign --verify --deep --strict` 严格签名校验通过。
- 线上 manifest 与线上 ZIP 已复核，版本、大小、SHA-256 均与本地产物一致。

## 已通过

- 工程重新生成成功，Debug、Release 与 Xcode Analyze 均通过。
- 1.2.1 的 Apple Translation Framework 版本已验证；当时的包不含 NLLB、CTranslate2、SentencePiece。1.2.2 已按新需求恢复 NLLB 可选项与本机运行时。
- 转录后 GUI 英译中通过，译文可切换显示并进入恢复快照。
- Apple Translation CLI 英译中通过，输出“你好，世界”，退出码为 0。
- CLI 主线程改为泵送 RunLoop，避免 Translation Framework 回调因主线程信号量等待而死锁。
- 实时字幕页可选择 Apple 本地识别或 Whisper Metal。
- Whisper 实时字幕模型菜单展示全部模型、大小与安装状态；Large v3 Turbo Q5 / 完整版均正确显示为已安装。
- Whisper 模型下载可仅服务实时字幕选择，不会改变普通转录默认引擎。
- Whisper Large v3 Turbo Q5 使用内置 Metal library 与 Apple GPU 完成中文测试音频识别并导出双段 SRT。
- SenseVoice → Apple Translation → 英文 SRT 通过，原时间轴得到保留。
- Apple Translation 首次低延迟语言组合会显示 macOS 官方语言包下载界面。
- 旧恢复快照中的 NLLB `model` 字段可被忽略并继续读取目标语言与既有译文。
- 1.2 功能版本提升为 1.2（6）；翻译交互热修复为 1.2.1（7）；本轮功能与布局更新为 1.2.2（8）。
- 1.2.1 Release `.app` 为 arm64，深度严格签名校验通过，包内无 NLLB/CTranslate2 目录；1.2.2 Release 同样通过严格签名校验，并包含恢复后的 NLLB 本机运行时。
- 线上 manifest、实际下载 ZIP 的版本、大小、SHA-256 与压缩包完整性均通过复核。
- 桌面现有 1.1（5）已完成“检查更新 → 下载 → 安装 → 自动重新打开”端到端验收；重启后为 1.2（6），再次检查显示最新版本。
- 桌面现有 1.2（6）已完成“检查更新 → 下载 → 安装 → 自动重新打开”端到端验收；重启后为 1.2.1（7），再次检查显示最新版本。
- 用户在 Codex Monitor 批准/完成本机 TCC 授权后，桌面版 `/Users/mattlixinchen/Desktop/Apps/声迹.app` 已复核为 1.2（6）。
- 实时字幕麦克风实机补验通过：在桌面版中选择 Apple 本地识别 + 麦克风，关闭实时翻译后启动字幕；通过 Mac 扬声器播放中文测试音频，悬浮字幕窗口成功出现并识别出“请。”，无麦克风 TCC 错误。
- 实时字幕 Mac 声音实机补验通过：在桌面版中选择 Whisper · Metal + Mac 声音 + Large v3 Turbo Q5，播放本机 AIFF 测试音频，悬浮字幕窗口成功识别出“现在正在播放 来自这台 MAC 的声音”“实时字幕应该显示这一段中文内容”等片段，无屏幕/系统音频 TCC 错误。
- 实时字幕模型选择实机路径通过：Apple 本地识别与 Whisper · Metal 可在主界面切换，Whisper 模型菜单显示 Large v3 Turbo Q5 已下载并可用于实时字幕。

## 首次运行权限

- 麦克风实时字幕需要用户批准麦克风权限。
- Mac 声音实时字幕需要用户批准“屏幕与系统音频录制”权限。
- Debug 临时签名包的 Mac 声音启动路径已验证能正确报告 TCC 拒绝；系统权限不能由无人值守验收代替用户批准。
- Release 桌面版在用户授权后已完成 TCC 实机补验：麦克风与 Mac 声音均可进入实时监听并产生字幕。观察项：Apple 本地识别 + Mac 声音本轮能进入监听且无 TCC 错误，但两段测试音频未产生可见字幕；最终 Mac 声音转写通过路径为 Whisper · Metal。
- 低延迟 Apple Translation 语言包由 macOS 下载并可在系统设置管理；高质量英中语言包已实测可用。

## 验收样本

- `R:/acceptance/whisper-live-zh.wav`
- `R:/acceptance/whisper-live-zh.srt`
- `R:/acceptance/apple-translation-en.srt`

## 发布产物

- `dist/声迹.app`
- `dist/声迹-macOS26-arm64.zip`
- `dist/localscribe-update/update.json`

最终 SHA-256 与文件大小以同目录及线上 `update.json` 为准。

- 上一版本 1.2.2 ZIP SHA-256：`b4b4b66a136851ff3f49e80ad599d7a1ab7531966e95b0c7c947f0fa4928002a`
- 上一版本 1.2.2 ZIP 大小：`20,802,304` 字节
- 1.2.3 ZIP SHA-256：`f8f3f2c8c0f75d21df4650bf6d4d33180b4f07277d608a404a5a349e299f6c4a`
- 1.2.3 ZIP 大小：`20,842,562` 字节
- 线上 manifest：`https://lixinchen.ca/localscribe/update.json`
